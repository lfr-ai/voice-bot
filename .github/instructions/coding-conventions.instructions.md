---
description: Python coding conventions
applyTo: "**/*.py"
---

# Coding Conventions

- Use Python 3.13-compatible syntax and full type hints.
- Use structured logging instead of `print` in application code.
- Prefer explicit exception handling and preserve context with `raise ... from e`.
- Keep module-level constants in `UPPER_SNAKE_CASE`.
