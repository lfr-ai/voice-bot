---
description: Shell and PowerShell conventions for scripts and hooks
applyTo: "**/*.{sh,ps1}"
---

# Shell Conventions

- Use strict mode in shell scripts (`set -eu` at minimum).
- Quote variables and paths.
- Keep `.sh` and `.ps1` behavior aligned when both variants exist.
