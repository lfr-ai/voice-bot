version: '3.8'
services:
  app:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: bash -c "source .venv/bin/activate || true && uvicorn voice.interaction.main:app --host 0.0.0.0 --port 8000"
